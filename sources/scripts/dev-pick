#!/usr/bin/env bash
# dev-pick - Pick a project via rofi and open it with yawn
# Bound to $mod+p in i3
set -euo pipefail

selected="$(yawn list ~ --pretty | rofi -dmenu -p project -i)"
[[ -z "$selected" ]] && exit 0

path="$(yawn resolve -P ~ "$selected")"
yawn open "$path"
